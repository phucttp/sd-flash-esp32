# ESP MultiFlasher (Muti) — custom partition table for 4MB flash.
# Bigger factory app (3MB) so the image fits once Bluedroid BLE is linked in
# for the WiFi-over-BLE provisioning fallback (the old 2MB single-app-large
# preset was too tight).
# Name,     Type, SubType, Offset,   Size,     Flags
nvs,        data, nvs,     0x9000,   0x6000,
phy_init,   data, phy,     0xf000,   0x1000,
factory,    app,  factory, 0x10000,  0x300000,
